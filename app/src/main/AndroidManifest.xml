<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codebounds.tofyapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/tofy_keys_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tofy_keys_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".configuracion.SeguridadConfiguracionActivity"></activity>
        <activity android:name=".configuracion.InformacionActivity" />
        <activity android:name=".configuracion.ConfiguracionActivity" />
        <activity android:name=".claves.ClavesActivity" />
        <activity android:name=".registro.SuccessSeguridadActivity" />
        <activity android:name=".registro.PinActivity" />
        <activity android:name=".registro.SeguridadActivity" />
        <activity android:name=".registro.RegistroActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>